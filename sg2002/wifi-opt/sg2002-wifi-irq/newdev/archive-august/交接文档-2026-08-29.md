# 工作交接：SG2002 WiFi IRQ 分支（2026-08-29 会话，含 PR 准备）

- 分支：`sg2002/wifi-irq`（worktree `/workspace/wt-sg2002-wifi-irq`，HEAD == dev == 7c5bbd133，改动工作区未提交 → 本会话末将提交快照）
- 板卡：LicheeRV Nano（SG2002），板载 Aic8800D80 WiFi（SDIO1）
- 上游交接：`www/newdev/交接文档-2026-08-28.md`（背景、前三轮板测、GET_MAC 之前的进展）
- 追踪文档：`www/newdev/问题修复追踪.md`（持续更新，本会话已追加全部新记录）

---

## 一、本会话完成的工作（按时间序）

1. **接手**（用户"阅读www/newdev内容了解工作"）：核对工作区与交接文档一致，评估通过。
2. **TODO 4.1 落地**：count raw 改"非零才打 info"（mailbox.rs）+ 判别镜像构建（053855）。
3. **wc2.log 板测判别**：AP 5s 窗口 count 恒 0 → OTHER 位排除；StackStart CFM 到达 → 主程序活着；对照旧驱动发现**全零 MAC 假设**。启动期 84 行 flow 之谜解开（UploadMain 330 块 + 带宽丢行）。
4. **GET_MAC 修复**（启动期 GetMacAddress 阶段，测试先行）：wc3.log 验证生效（CFM 到达 + eth0 mac=38-7a-cc-9b-2c-c8），但 AP 仍无响应 → **全零 MAC 假设证伪**。
5. **LMAC 前置 4 命令**（对照旧驱动 start_ap_open）：wc4.log 验证**无效**（第一条 MM_RESET 即无应答）。
6. **Wake 相位**（对照厂商 aicwf_sdio_wakeup：wakeup_reg 0x11 + sleep_reg bit4 轮询）：wc5.log 验证**无效**（bit4 置位但命令不处理）。
7. **wc5 深度分析**（用户纠正逐字符回显不正常）：定位两个独立问题并修复：
   - 修复 A（fail 事件可靠传播）：30s 兜底延迟 → 立即返回。**wc6 验证生效**（30s→5s）。
   - 修复 B（card interrupt 先读 CCCR 0x05 pending bitmap 清除）：**wc6 验证无效**（问题不变）。
8. **wc6.log**：问题不变（AP 21.06s 提交 → 26.07s 5s 超时返回）。**用户决策：停止修复，转入 PR 准备**。

## 二、当前状态

### 核心问题（未解决，停止调试）

**Ready 态第一条控制命令（MM_RESET/ADD_IF/SM_CONNECT）固件无应答**：wake bit4 置位（SDIO 层活）、int pending 清除无效、flow=124、count 恒 0、5s 超时 → 设备 Failed（不可逆）。

已排除：消息 ID/payload 布局/帧格式（与厂商逐字段一致）、命令顺序（前置 4 命令补齐后仍无应答）、全零 MAC（GET_MAC 后仍无应答）、OTHER 位恒置（count 恒 0 无 0x8x）、固件睡眠（wake 后 bit4 置位仍不处理）、int pending 未清（清除后仍无应答）。

剩余候选（后续调试方向）：
1. 固件主程序在 StackStart 后进入某状态，只响应启动期消息（GET_MAC 在 ArmChipInterrupt 前成功，Ready 态一切命令失败——**时间边界是 ArmChipInterrupt**，可沿此查：中断使能写值/时机是否触发固件状态切换）；
2. 固件等待某条未发送的启动期消息（如 ME_SET_PS_MODE、MM_VERSION）；
3. 厂商驱动启动完成后有额外的 host 动作未建模（对照厂商 rwnx 的 post-boot 流程逐条核对）。

### 生效修复（板测验证，PR 范围）

7 项，见 `www/newdev/有效修复总结-PR准备.md`：init 循环推进、错误透传、写寄存器形状契约（4 点）、revision 高半解析、GET_MAC、日志降级、fail 可靠传播。

### 未生效修复（保留本分支，排除出 PR）

LMAC 前置 4 命令、Wake 相位、int pending 清除、邮箱排空/坏帧重试加固、count raw 非零 info。详见有效修复总结文档的排除表。

## 三、PR 准备（已完成，待推送）

1. ✅ 有效修复总结文档：`www/newdev/有效修复总结-PR准备.md`
2. ✅ 快照提交：`3e08c4150`（sg2002/wifi-irq，排除 Cargo.lock）
3. ✅ 副本分支 `sg2002/wifi-irq-initFix` + worktree `/workspace/wt-sg2002-wifi-irq-initFix`
4. ✅ 副本删减至 6 项功能性修复 + 注释整理（中文学习注释/本地引用/新增日志全部移除）
5. ✅ 验证 + 分主题提交（4 条，见有效修复总结文档"PR 分支状态"节）
6. 待办：PR 描述撰写（中文正文，按项目规范）→ 提交 PR → 推送（需用户批准）

## 四、关键索引

### 板测日志（www/logs/）

- 1-8.log：早期板测（60s 死锁 → 开机成功）
- wc2.log：判别（OTHER 排除、全零 MAC 假设）
- wc3.log：GET_MAC 生效 + ADD_IF 仍无应答（假设证伪）
- wc4.log：前置 4 命令无效（MM_RESET 无应答）+ 逐字符回显
- wc5.log：STA 30s 延迟（fail 传播问题定位）+ 持续逐字符
- wc6.log：5s 返回（fail 传播生效）+ 问题不变

### 代码（工作区全部改动，19 文件）

- sdmmc-protocol：init.rs + tests（有效 1、6）
- ax-net：queue_runtime（有效 2）
- aic8800：request/model/startup/firmware/progress（有效 3/4/5）、mailbox（有效 6 部分 + 排除部分）、control（排除）、rdif owner/output/progress（有效 6/7 + 排除部分）、data_plane/operation（排除）
- Cargo.lock：无关，提交时排除

### 文档（www/newdev/）

- `问题修复追踪.md`（持续追踪，全部板测记录）
- `有效修复总结-PR准备.md`（PR 范围界定，本会话新建）
- `交接文档-2026-08-28.md`（上一会话交接）
- `改动审查与wifi-panic根因报告.md`（§4 已 superseded）
- `VendorSetup响应形状失配.md`（真因记录）

### 镜像（.sg2002-build/output/，latest.img 软链）

- 最后板测：`sg2002_starryos_wifi-irq_20260828-193627_kernel-20260829-053855_kernel-20260829-055930_kernel-20260829-061039_kernel-20260829-062932_kernel-20260829-065227.img`（含全部修复；wc6 板测用）
- 全部镜像 p2 完好性均验证（magic + sha256 抽查）

### 构建命令

```
cargo test -p aic8800 --features "host-test rdif"
cargo test -p sdmmc-protocol --features rdif
cargo xtask clippy --package aic8800
cargo xtask starry build -c os/StarryOS/configs/board/licheerv-nano-sg2002-wifi.toml
/workspace/.claude/skills/sg2002-image-build/scripts/sg2002-image-build.sh update-kernel <base.img> --kernel target/riscv64gc-unknown-none-elf/release/starryos.bin
```

## 五、防歧义备注

1. "wake 后 bit4 置位"证明的是固件 SDIO 层响应了唤醒寄存器写入，不代表固件主程序在正常处理命令——两件事独立。
2. wc6 的 5s 返回是修复 A（fail 传播）的生效证据，不是核心问题缓解。
3. 有效修复总结文档中的"板测证据"均对应具体日志行，删除任何一项前先核对。
4. 副本分支整理时 mailbox.rs 恢复 dev 原状后，GET_MAC/revision 的测试需要按 dev 相位结构（无 drain）重写到 mailbox.rs 新测试区。
5. dev 原有中文注释若不在本会话 diff 内，不属于删除范围；diff 内出现的中文注释全部删除。
6. 提交快照前跑 fmt 确认（本会话最后一次 clippy/fmt 全过）。
